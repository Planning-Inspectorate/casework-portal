trigger: none

pr: none

resources:
  pipelines:
    - pipeline: terraform-ci
      source: Infrastructure PR
      trigger:
        branches:
          include:
            - main
  repositories:
    - repository: templates
      type: github
      endpoint: Planning-Inspectorate
      name: Planning-Inspectorate/common-pipeline-templates
      ref: refs/tags/release/3.24.0

extends:
  template: pipelines/terraform_plan_apply.yml@templates
  parameters:
    environments:
      - name: Dev
      - name: Prod
        dependsOn:
          - Dev
    serviceConnectionPrefix: Azure DevOps Pipelines - Casework Portal - Infrastructure
    storageAccountName: pinssttfstatecwp
    resourceGroupName: pins-rg-shared-terraform-uks
    containerPrefix: terraform-state-casework-portal-
    workingDirectory: $(Build.Repository.LocalPath)/infrastructure
    environmentVarFilePath: $(Build.Repository.LocalPath)/infrastructure/environments
